# Sanctuary（うつ病AI）要件定義書

作成日: 2026年8月31日

---

## 1. プロダクトの概要

| 項目 | 内容 |
|---|---|
| プロダクト名 | **Sanctuary**（3分間の非言語・自立支援オーディオシェルター） |
| 一言でいうと | うつ病・トラウマ・極度の精神的疲弊で言葉や思考がフリーズしている人に向けた、心理的マニピュレーションを一切排した「安全基地」 |
| ターゲット | 言葉にする気力すら残っていない状態の人 |
| 収益モデル | 月額500円のサブスクリプション。無料枠は3分間、24時間ごとにリセット |

---

## 2. 核となる思想（絶対の禁止事項）

- AI側から「こうすべき」「頑張って」といった解決策・アドバイス・ポジティブの押し付けを、システムプロンプトレベルで禁止
- タルムードの「過剰な介入の禁止」「個の尊厳の尊重」を裏の倫理とし、ユーザーの人生の主導権・決定権を奪わない
- 起動時の自己紹介で、重い感情表現や依存を誘発するワードを使わない
- ユーザーの本名は取得しない。最初の1往復で「ニックネーム」だけを尋ね、以降その名前だけで呼びかける

## 3. 対話の原則

1. **圧倒的な受容と全肯定**: 「しんどい」「疲れた」という言葉を否定せず丸ごと受け止める。まず存在と頑張り自体を肯定する
2. **熱量に応じた動的対応**:
   - 低熱量（ただ休みたい）→ アドバイスなし、受容のみ
   - 高熱量（ユーザー自身が「どうしたらいいか」と問うてきたとき）→ 答えを押し付けず、小さなヒントをそっと手渡す
3. **言葉には言葉で応える**: 「非言語」は言葉が出ない状態への選択肢であり、対話そのものを拒む意味ではない
4. **目的**: 一時的な気休めではなく、自立思考を内側から引き出すこと。ただし焦らせない

具体的なプロンプト本文は `lib/system-prompt.ts` を参照。

## 4. 安全網（希死念慮への対応）— 設計の経緯

当初案（AIが会話中に一切アドバイスを出さない）のままだと、希死念慮のサインが出たときにも何もできなくなってしまうため、以下の**唯一の例外ルール**を組み込んだ。

- **明確な表現**（「死にたい」「死のうと思う」「もう死ぬ」等、方法への言及を含む直接的な言葉）→ **1回で即座に**発動
- **曖昧・比喩的な表現**（「消えたい」「しんどい」「もう無理」等）→ **3回の積み重ね**で発動

発動条件を満たしても、**会話は止めない・AIのセリフとしても一切言わせない**。代わりに、画面の隅に「→ もしもの時は」という小さな静かなリンクが現れるだけ（`app/page.tsx`の`showResource`）。押さない限り何も起きないので、会話の雰囲気は一切壊れない。

案内先は「よりそいホットライン」（0120-279-338、24時間・無料、チャット相談窓口としての性質もあり、電話をかける気力が無い人にも配慮している）。**本番運用前に、この番号が現在も有効か必ず確認してください。**

## 5. 技術構成

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js（App Router）+ TypeScript + Tailwind CSS |
| AI | Anthropic Claude API（`/api/chat`が中継。サーバー側でのみAPIキーを保持） |
| 音響 | Web Audio API によるアンビエントパッド生成（`lib/audio-engine.ts`） |
| 決済 | Stripe決済リンク（サブスクリプション） |
| ホスティング | Vercel（フロント・APIとも同一プロジェクトでデプロイ可能） |

### 5-1. なぜローカルAI（WebLLM）ではなくClaude APIにしたか

当初案は「サーバーコストほぼゼロ」を目指し、ブラウザ内で完結するローカルAI（WebLLM等）を想定していた。しかし月額課金モデルが決まったことで、API費用を賄える前提に変わったため、**対話の質と安全性（特に希死念慮判定の精度）を優先し、Claude APIを使う方針に変更した**。軽量なローカルモデルは、この繊細な用途には応答品質・安全面で力不足になるリスクが高いと判断したため。

### 5-2. 「AIが息づかいにハモる」機能について、正直な見立て

当初案にあった「ユーザーの息づかい・ハミングにブラウザ上でリアルタイムに美しくハモる」は、現状のブラウザ技術（WebGPU/WASM）では期待される品質での実現が困難と判断した。**第一段階として、マイクで拾った「声・息の気配（音量）」に応じて、生成中のアンビエント音の豊かさがゆっくり変化する**、より現実的な「気配への呼応」を実装した（`AmbientEngine.enablePresenceSensing()`。まだUIには接続していない任意機能）。マイクの生音声は録音・送信せず、音量だけをその場で見るのみ。

## 6. 設定方法（あなたが行う作業）

### 6-1. Anthropic APIキーの取得

1. [console.anthropic.com](https://console.anthropic.com) でアカウントを作り、支払い方法を登録する
2. **利用上限額を必ず設定する**（Usage limits）。このアプリは1日あたりの会話量に上限を設けていないため、こちらでの上限設定が実質的な歯止めになる
3. APIキーを発行し、控えておく

### 6-2. ローカルでの動作確認

1. `.env.local.example` を `.env.local` にコピーし、`ANTHROPIC_API_KEY` に控えたキーを貼り付ける
2. `npm run dev` を実行し、`http://localhost:3000` を開く

### 6-3. Vercelへのデプロイ

1. このプロジェクトをGitHubリポジトリにする（まだであれば）
2. [vercel.com](https://vercel.com) でアカウントを作り、リポジトリをインポートする（Next.jsは自動検出されるので設定不要）
3. プロジェクトの「Settings」→「Environment Variables」で `ANTHROPIC_API_KEY` を登録する
4. デプロイ完了後に発行されるURLが本番アドレスになる

### 6-4. Stripe決済リンク（月額500円）の設定

1. [dashboard.stripe.com](https://dashboard.stripe.com) で商品を作成し、**「継続課金（サブスクリプション）」で月額500円**の価格を設定する
2. 決済リンクを作成し、「支払い後」を「別のウェブサイトに移動させる」に設定。移動先URLを
   `https://（あなたのVercelドメイン）/?paid=1&session_id={CHECKOUT_SESSION_ID}`
   にする
3. 発行された決済リンクを `lib/billing-config.ts` の `STRIPE_SUBSCRIPTION_LINK` に貼り付ける

**⚠️ 正直な注意点**: 現在の実装は「決済完了後に戻ってきたら解放する」方式（batuge-muのbilling.jsと同じ簡易版）で、サブスクリプションが解約された後もこの端末では使え続けてしまう。Webhookでの検証は将来課題。

## 7. アバター・音声演出（2026年9月3日、5種に拡張・プレミアム音声対応）

選択したアバター（犬／猫／くま／男性／女性の5種）を全画面背景に表示し、AIの発話・環境音のハミングに合わせて口・視線・眉が動く演出を実装した。

| 項目 | 内容 |
|---|---|
| アバターの見た目 | `components/Avatar.tsx`。外部の画像・動画素材は使わず、SVGで手続き的に描画（軽量・追加アセット不要・実在人物に似せる懸念もない） |
| 表情の演出 | 瞬き（`avatar-blink`）に加え、視線がゆっくりドリフトする瞳（`pupilLRef`/`pupilRRef`）、発話にあわせてわずかに動く眉（`browLRef`/`browRRef`）、呼吸で揺れる胸元を実装 |
| 発話音声（2経路） | `lib/voice-engine.ts`（`VoiceEngine`）。①プレミアム: `/api/tts`（OpenAI TTS、`OPENAI_API_KEY`設定時のみ）→ 実音声をWeb Audio APIの`AnalyserNode`に通し、**本物の音量（RMS振幅）に基づく**リアルタイムのリップシンクを行う。②無料フォールバック: ブラウザ標準のWeb Speech API。プレミアムが未設定・失敗した場合は自動でこちらに切り替わる（課金なしでも今まで通り動く） |
| 音声波形の表示 | `components/WaveformBars.tsx`。ヘッダーに5本のバーを表示し、プレミアム経路では実際の周波数データ、フォールバック時は口の開き具合から生成した疑似波形で動く |
| 画面レイアウト | アバターを`position: fixed`の全画面背景（`-z-10`）に配置し、対話UIは半透明の黒レイヤー（`bg-black/35`）を重ねた前面パネルとして表示。背景は暖色系のグラデーション（`#241c18`→`#07070a`）に調整 |

### 正直な見立て（重要）

- **「本物の音量に基づくリップシンク」は、プレミアム経路（`OPENAI_API_KEY`設定時）でのみ実現している**。ブラウザ標準のSpeechSynthesisは合成した音声波形を仕様上Web Audio APIに渡せない（AnalyserNodeで直接解析できない）ため、無料フォールバック側では引き続き`boundary`イベント（発話中の単語の区切り）を使った疑似リップシンクを採用している。実機（Chrome）でプレミアム・フォールバックの両方とも、実際に音声の強弱に応じて口が開閉することを確認済み
- プレミアム経路はOpenAIのTTS API（`gpt-4o-mini-tts`）を1発話ごとに呼び出すため、**利用量に応じて課金が発生する**（設定方法は下記）。設定しなければ今まで通り無料で動く
- `boundary`イベントの発火頻度・対応状況はブラウザ依存（Safari/Firefoxは制限が大きいことが知られている）。フォールバック側は一定時間イベントが来ない場合の簡易対応（ランダムなパルス）も維持している
- **アバターの絵柄について**: 「イケメン男性・美女」「圧倒的なリアリティ」というご依頼だったが、手描きのSVGでは写真的なリアリズムは技術的に出せない（実写風にするには、実在の人物に似せかねないAI生成画像や、著作権的にクリアな素材の調達が必要になり、いずれもこのアプリの方針とは相性が悪いと判断した）。代わりに、視線ドリフト・瞬き・表情の変化・呼吸を重ねることで「静止画ではなく生きている感じ」を出す方向に振っている。より写実的な見た目が必要な場合は、素材（イラスト・3Dモデル等）を別途ご用意いただければ、その素材に対してリップシンクの仕組み自体はそのまま流用できる
- 男性・女性アバターは、既存の犬・猫と同じ土台（頭・目・眉・口の構造）に、髪型と肌色だけを差し替える設計にした（`components/Avatar.tsx`の`Topper`関数と`SPECIES_CONFIG`）。特定の実在人物を想起させない、一般的な記号的表現にとどめている

### OpenAI TTS（プレミアム音声）の設定方法（任意）

1. [platform.openai.com](https://platform.openai.com) でアカウントを作り、支払い方法を登録する
2. 「API keys」から新しいAPIキーを発行する
3. `.env.local`（ローカル）または Vercel の Environment Variables（本番）に `OPENAI_API_KEY` として登録する
4. 設定するだけで自動的にプレミアム経路が使われるようになる（コード変更不要）。料金の目安・上限設定は[platform.openai.com](https://platform.openai.com)の使用量ページで確認・設定してください

## 7-2. 簡易ビゼーム(母音の口形)morphへの強化（2026年9月4日追記）

従来は「音声の音量(RMS振幅)に応じて口の楕円の高さ(ry)だけを変える」実装
だったが、これだと母音による口の「形」の違い（お・う＝丸い、い・え＝横に
広い、あ＝縦に開く）が表現できていなかった。プレミアム音声経路
（`OPENAI_API_KEY`設定時）では実際の音声波形をAnalyserNodeで解析できる
ため、以下を追加した。

- `lib/voice-engine.ts`: RMS振幅（口の開き=`mouthOpenness`）に加えて、
  周波数分布の**重心**（低域寄りか高域寄りか）から`mouthWidth`
  （0=丸い口寄り、1=横に広い口寄り）を推定するようにした
- `components/Avatar.tsx`: 口を単なる楕円のスケールではなく、
  `mouthOpenness`と`mouthWidth`の2軸からベジェ曲線で輪郭を生成する
  `mouthPath()`に置き換えた。母音ごとに異なる口の概形が滑らかにmorphする

**正直な注意点**: これは音声認識による正確な音素判定ではなく、周波数重心
という単純な指標からの近似（あくまで見た目のバリエーションを増やすための
ヒューリスティック）。無料フォールバック経路（Web Speech API）は実際の
音声波形を解析できない仕様上の制約があるため、`mouthWidth`は発話中に
ゆっくり往復させる疑似演出のまま（実際の母音とは連動しない）。

### 写実アバター（男性・女性）の追加（2026年9月5日追記）

上記の判断を踏まえ、実在の作品・人物に似せない指示のもとGemini(Google)で
生成し、本人が確認した写真を、男性・女性アバターに採用した。

- `components/Avatar.tsx`: `PHOTO_SPECIES`に登録されたspecies（man/woman）は、
  従来のSVG描画ではなく`public/avatars/`配下の写真を全画面表示する
- 口の形を複数枚生成する方式は、生成のたびに顔立ちが微妙にズレて
  不自然になりやすいため採用せず、話している間だけ画面全体がごく
  やわらかく発光する演出にした（`lib/audio-engine.ts`の「派手な反応には
  しない」という既存方針を踏襲）
- 写真の対象がない犬・猫・くまは、引き続き既存のSVG描画のまま
- 静止画はゆっくりズームする(`avatar-photo-kenburns`)ことで、動画のような
  生きている気配を出している（`prefers-reduced-motion`時は停止）

### 「圧倒的なリアリティ」（写実的な人物アバター）について

今回、「Live2D/VRM等の先進的なオープンソース技術を使った、より圧倒的な
リアリティを持つアバター」という方向性の依頼もあったが、今回はSVGの
ビゼーム強化にとどめ、写実的な3D/画像ベースのアバターへの置き換えは
行っていない。理由は要件定義書 セクション7の「正直な見立て」で述べた
判断（実在人物に似せかねない生成画像・素材の調達を避ける方針）が
現在も有効だと考えたため。もしLive2D CubismやVRM（three-vrmライブラリ等）
による本格的な表情制御に進む場合は、次のいずれかの素材調達方針を先に
決める必要がある。

- 商用利用可能なライセンス（CC0等）のVRM/Live2Dモデルを調達する
- 自前でイラストレーターにオリジナルキャラクターを発注する
- AI画像生成を使う場合、実在人物に類似しないよう十分距離を取った
  デザイン仕様を先に固める

いずれも素材のライセンス確認や発注が必要で、コードの実装だけでは
完結しない意思決定のため、今回は見送った。方針が決まれば、
`VoiceEngine.getMouthOpenness()`/`getMouthWidth()`の出力はそのまま
Live2DのパラメータやVRMのブレンドシェイプ制御にも流用できる設計にして
ある。

## 8. 未実装・今後の課題

- マイクの「気配への呼応」機能はエンジン側のみ実装済み、UIから有効化するボタンは未接続
- Stripeサブスクリプションの解約検知（Webhook）
- クライシス対応の文言・番号は、公開前に専門家の確認を受けることを強く推奨する
- アバターの種類は犬・猫・くま・男性・女性の5種。増やす場合は`components/Avatar.tsx`の`SPECIES_LABEL`/`SPECIES_CONFIG`/`Topper`に追加する
- プレミアム音声（OpenAI TTS）は、一度失敗すると同一セッション中はフォールバックに固定される設計。API側の一時的な障害から自動復帰する仕組みは未実装
