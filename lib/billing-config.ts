/* =========================================================
   💎 月額課金の設定（Stripe決済リンク・サブスクリプション）
   ---------------------------------------------------------
   ここに、あなた自身のStripe決済リンク（サブスクリプション/
   月額500円の商品で作成したもの）を貼り付けると、課金が
   始まります。作り方は 要件定義書.md を参照してください。

   ⚠️ 忘れずに：Stripe側の決済リンク設定で「支払い後」を
   「別のウェブサイトに移動させる」にし、移動先URLを
     https://（あなたのVercelドメイン）/?paid=1&session_id={CHECKOUT_SESSION_ID}
   にしてください（{CHECKOUT_SESSION_ID} はそのまま入力すればOK）。

   まだ設定していない間は、アップグレードボタンを押すと
   「設定が必要です」という案内が出るだけで、他の機能には
   一切影響しません。無料の3分間はそのまま使えます。
   ========================================================= */

export const STRIPE_SUBSCRIPTION_LINK = "https://buy.stripe.com/YOUR_SUBSCRIPTION_LINK";
