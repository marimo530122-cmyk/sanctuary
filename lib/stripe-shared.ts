/* =========================================================
   Stripe関連で、クライアント・サーバーの両方から参照する定数
   ---------------------------------------------------------
   秘密情報は含まない（APIキー等はサーバー専用のlib/stripe-server.tsへ）
   ========================================================= */

export const SESSION_ID_PATTERN = /^cs_(test|live)_[A-Za-z0-9]{16,}$/;
export const CUSTOMER_ID_PATTERN = /^cus_[A-Za-z0-9]+$/;
